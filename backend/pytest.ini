[pytest]
testpaths = tests
addopts = -m "not live and not slow"
markers =
    live: needs a running Ollama server with the default model pulled
    slow: loads the faster-whisper speech model (first run downloads ~1.5 GB)
